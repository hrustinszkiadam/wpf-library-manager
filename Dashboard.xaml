<Page x:Class="library_manager.Dashboard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:library_manager"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Dashboard" FontSize="10">

    <Viewbox Stretch="Uniform">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Vertical" Grid.Column="0" Width="100">
                <StackPanel Orientation="Vertical" Name="AdminControls" Visibility="Hidden">
                    <Button Content="Felhasználók" Width="75" Click="UserListPage"/>
                </StackPanel>
                <Button Content="Kijelentkezés" Width="75" Click="Logout"/>
            </StackPanel>
            <Frame Name="DashboardFrame" Grid.Column="1" Width="200" />
        </Grid>
    </Viewbox>
</Page>
